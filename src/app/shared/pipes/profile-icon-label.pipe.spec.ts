import { ProfileIconLabelPipe } from './profile-icon-label.pipe';

describe('ProfileIconLabelPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileIconLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
